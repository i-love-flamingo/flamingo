flamingo:
  image: go:1.9
  script:
  - export GODIR="${GOPATH}/${GODIR}"
  - go get -u github.com/golang/dep/cmd/dep
  - ln -sfv "$(pwd -P)" "${GODIR}"
  - cd "${GODIR}"
  - dep ensure -vendor-only
  - go test ./...
  cache:
    paths:
    - vendor/
