language: go

go:
  - 1.x
  - 1.11.x

env:
  - GO111MODULE=on

install: true

script:
  - golint -set_exit_status $(go list ./...)
  - go vet ./...
  - gocritic check-project .
  - go test -v ./...
  - go test -v -race ./...